# .NET
apps/api/**/bin/
apps/api/**/obj/
apps/api/**/*.user

# Node / Next.js
apps/web/node_modules/
apps/web/.next/
apps/web/out/
apps/web/*.tsbuildinfo

# Environment files with real secrets
.env
.env.local
apps/web/.env.local
apps/api/**/appsettings.Local.json

# Editor / OS
.vs/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Bicep compiled output (kept out of source control; generated by CI/deploy)
infrastructure/bicep/**/*.json
!infrastructure/bicep/**/*.parameters.json

# Deploy script gets real secrets pasted into it locally -- never commit
deploy.ps1

# Claude Code local tooling state (agent worktrees, etc.) -- not project content
.claude/
